{
  "name": "themename",
  "version": "1.0.0",
  "scripts": {
    "styles": "sass --load-path=templates scss/styles.scss dist/styles.css",
    "build": "npm run -s styles",
    "test": "npm run -s build"
  },
  "dependencies": {
    "sass": "^1.53.0"
  },
  "devDependencies": {
  }
}
